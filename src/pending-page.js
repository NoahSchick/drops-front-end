import React from "react"

function PendingPage(props) {
  return (
    <div>
      <h1>Your transactions is pending</h1>
      <a href={`https://etherscan.io/}`}>etherscan</a>
    </div>
  )
}

export default PendingPage
